{
    "configurations": [
        
        {
            "name": "Python: File",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "cwd": "${fileDirname}",
            "justMyCode": true,
            "purpose": ["debug-in-terminal"]
        }
    ]
}